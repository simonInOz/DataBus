var express = require("express");
var router = express.Router();
// var auth = require("../modules/auth");

/* GET home page. */
// router.get("/", auth, (req, res) => {
//   res.render("index", { title: "PSMS-svr" });
// });

module.exports = router;
